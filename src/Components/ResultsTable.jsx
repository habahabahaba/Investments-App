// 3rd party:
// Redux RTK:
// Store:
// React Router:
// React:
// Context:
// Hooks:
// Components:
// CSS:

function ResultsTable({ results }) {
  // JSX:
  return <p>{JSON.stringify(results, null, 2)}</p>;
}

export default ResultsTable;
